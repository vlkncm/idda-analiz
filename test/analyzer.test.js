const test=require('node:test');const assert=require('node:assert/strict');const {clamp,summarizeH2H,makeRecommendation,predictMatchResult}=require('../analyzer');
test('puanlar yüzde aralığında tutulur',()=>{assert.equal(clamp(130),100);assert.equal(clamp(-4),0)});
test('muhtemel maç sonucu 1/0/2 kodlarından biriyle döner',()=>{const base={played:10,draws:3};const x=predictMatchResult({scores:{homeAdvantage:74},home:base,away:base,h2h:{played:6,draws:2}});assert.equal(x.code,'1');assert.deepEqual(Object.keys(x.probabilities),['0','1','2'].sort((a,b)=>Number(a)-Number(b)));assert.equal(Object.values(x.probabilities).reduce((a,b)=>a+b,0),100)});
test('H2H sonuçları doğru sayılır',()=>{const f=(h,a,hid=1,aid=2)=>({fixture:{status:{short:'FT'}},teams:{home:{id:hid},away:{id:aid}},goals:{home:h,away:a}});const x=summarizeH2H([f(2,1),f(0,0),f(1,3)],1,2);assert.deepEqual([x.homeWins,x.draws,x.awayWins],[1,1,1])});
test('karar motoru birden fazla pazar ve kaçınılacak pazar üretir',()=>{const x=makeRecommendation({scores:{homeAdvantage:70,over25:66,btts:64,firstHalf:40,secondHalf:70,confidence:76},home:{played:10},away:{played:10,losses:6},h2h:{played:6},injuriesAvailable:true});assert.equal(x.action,'SEÇENEKLERİ DEĞERLENDİR');assert.ok(x.picks.length>=3);assert.ok(x.primary);assert.ok(x.comboWarning)});
test('zayıf ve çelişkili göstergelerde pas geçilir',()=>{const x=makeRecommendation({scores:{homeAdvantage:52,over25:55,btts:54,firstHalf:43,secondHalf:40,confidence:70},home:{played:10},away:{played:10,losses:3},h2h:{played:3},injuriesAvailable:false});assert.equal(x.action,'PAS GEÇ');assert.equal(x.stake,'0 birim');assert.ok(x.avoid.length>=2)});
